CREATE TABLE emp_log (
  no		NUMBER PRIMARY KEY,
  empno		NUMBER,
  ename		VARCHAR2(30) NOT NULL,
  position   	VARCHAR2(30),
  dname		VARCHAR2(30),
  logtime	DATE
);

CREATE SEQUENCE log_seq
     INCREMENT BY 1
     START WITH 1
     CACHE 2;

DELETE FROM emp2 WHERE emp_type = 'Probation';
